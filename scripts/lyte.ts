namespace lyte {
  /* const resources = engine.resources([
    'instances',
    'sprites'
  ]) */

  import api = module('./lyte/api')

  // exports.objects = resources

  /* exports.init = (fps) => {
    api.init()
    melted.hammer.drop(resources)
    setInterval(engine.run, fps, api)
    setInterval(engine.burn, fps, melted)
  } */
}